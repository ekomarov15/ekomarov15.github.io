version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: oge
      POSTGRES_PASSWORD: oge
      POSTGRES_DB: oge

  backend:
    build: backend
    ports: ["3001:3001"]
    depends_on: [db]

  frontend:
    build: frontend
    ports: ["3000:3000"]
